//TODO: logging général dans la console & dans un fichier
// - créer une fonction log() qui prend en paramètre un message, et qui l'affiche dans la console et dans un fichier
// - effectue un console.log() du message
// - verifie l'existance du fichier log/{date}.log
// - s'il n'existe pas, le créer
// - s'il existe, l'ouvrir et ajouter une ligne avec le message et un dateLog() habituel
//
// un nouveau fichier log est donc créé chaque jour, lorsqu'un nouvel event est loggé
//

function log(message) {}
